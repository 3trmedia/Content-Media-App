# Shared Supabase project (same project as Ben's Improvement App and
# Business Ops App — do not create a separate project). Fill these in
# locally in .env.local (gitignored); never commit real values here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=
